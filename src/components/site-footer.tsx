export function SiteFooter() {
  return (
    <footer className="mt-auto border-t border-border">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 px-4 py-8 text-sm text-fg-muted sm:flex-row sm:items-center sm:justify-between sm:px-6">
        <p>Letlist watches WhatsApp housing groups so clients can search what is actually available.</p>
        <p className="text-fg-subtle">Listings are live until they are taken.</p>
      </div>
    </footer>
  );
}
